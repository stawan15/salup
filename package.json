{
  "name": "worklog-ai",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node scripts/local-server.mjs",
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --check public/app.js && node --check api/summarize.js && node --check api/config.js && node --check scripts/local-server.mjs"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^7.1.2"
  }
}
